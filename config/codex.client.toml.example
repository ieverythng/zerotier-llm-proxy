# Add this provider registry entry to ~/.codex/config.toml.
# It intentionally does not change the default model.

[model_providers.qwen36-zerotier]
name = "qwen36 Lucebox via Windows ZeroTier"
base_url = "http://10.88.140.94:18080/v1"
wire_api = "responses"

